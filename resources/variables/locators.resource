*** Variables ***

# Technically the same as variables.resource but in case these want to be separated
${locator}        //a[@class, 'someclass']
${muiResultTable}     //table[contains(@class, 'custom-table')]
